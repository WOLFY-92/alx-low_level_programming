#include <main.h>

void positive_or_negative(int i);

int main(void)
{
        int i;
        if (i > 0)
        {
                printf("%d is positive\n", n);
        }
        else if (i < 0)
        {
                printf("%d is zero\n", i);
        }
        else
        {
                printf("%d is negative\n", i);
        }
}
